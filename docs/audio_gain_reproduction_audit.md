All three fixed-spherical checkpoint evaluations reproduced the original generation and classification formulas but did not reproduce the archived rounded digit accuracies. No concrete implementation mismatch was found in the source audit. The cause of the residual accuracy differences remains unresolved. This audit used source inspection, JSON reads, and Python AST comparisons only; it performed no new numerical evaluation, tuning, or retraining.

Slurm job 763355 completed all three original-plus-rescaled evaluations in `outputs_audio_gain/fixed_spherical_empirical_gain_v1_attempt2`. The [final aggregate](../outputs_audio_gain/fixed_spherical_empirical_gain_v1_attempt2/aggregate.json) records `status="baseline_mismatch"` and `archived_baselines_reproduced=false`. After writing every per-seed measurement and the aggregate, the historical-reproduction guard intentionally raises an error and the command exits 1. All measurements and paired invariance checks are complete; the nonzero exit preserves the unresolved historical accuracy discrepancy.

| Training seed | Archived digit accuracy, rounded | Original Y, measured accuracy | Rescaled X, measured accuracy | Changed predictions Y→X | Final status |
| --- | ---: | ---: | ---: | ---: | --- |
| 8925 | 0.805 | 0.8025 = 1605/2000 | 0.8025 = 1605/2000 | 0 | Complete; archived accuracy mismatch |
| 1234 | 0.798 | 0.8005 = 1601/2000 | 0.8005 = 1601/2000 | 0 | Complete; archived accuracy mismatch |
| 7 | 0.828 | 0.8170 = 1634/2000 | 0.8170 = 1634/2000 | 0 | Complete; archived accuracy mismatch |

The measured accuracy mean across all three seeds is `0.8066666666666666`, with population standard deviation `0.007352248333370906`, for both original Y and rescaled X: `0.807 ± 0.007` at three decimals. These values come from the aggregate's full-precision per-seed measurements. The separate legacy-rounded aggregation uses `0.803, 0.800, 0.817` and has population standard deviation `0.007408703590297578`; both forms remain available in the result JSON. Across all 6,000 paired predictions, rescaling changes none.

The [seed-8925 result](../outputs_audio_gain/fixed_spherical_empirical_gain_v1_attempt2/seed_8925/eval.json) preserves both exact count-based accuracy and the legacy Float32 mean. The latter is `0.8025000095367432`, which the original three-decimal rounding rule renders as `0.803`. The archived `0.805` cannot be explained by using exact count-based accuracy instead of the legacy Float32 mean. The historical archive contains rounded values, so it does not establish the original exact correct count or the number of individual predictions that differ from this rerun.

For every seed, energy KS changes from `0.5923333333333334` to `0.021833333333333333` after independent empirical-gain rescaling. The post-hoc KS mean is therefore `0.021833333333333333` with population standard deviation zero. Original Y matches every archived energy comparison available in `stage2_3seed.json`: rounded KS, coverage above q95 and q99, coverage below q10, and PIT. Fixed-spherical outputs have nearly constant radii; matching these radial summaries does not establish that their generated directions are identical to the historical sample set. Each seed's paired Y→X check passes over all 2,000 samples, with zero changed predictions, unchanged accuracy, and finite logits within tolerance. For seed 8925, the maximum absolute logit difference is `1.7642974853515625e-05`.

The historical published fixed-spherical row, `0.810 ± 0.013`, remains historical evidence and must not be replaced with the rerun accuracy. Likewise, a new empirical-gain result must use its measured paired accuracy rather than borrowing the historical value. The completed three-seed addition and any figure must make the reproduction discrepancy explicit. No tuning, radius change, retraining, or result substitution is justified by this discrepancy.

The [source compatibility audit](../outputs_audio_gain/release_verification/source_compatibility.json) compared eight files directly against `git show d3006dc8ee61b2ab6e470115d3f21c778fefcf81:PATH`; every byte comparison passed. These files cover the original model, evaluation, classifier, data preparation, radial source, sphere utilities, tangent projection, and spherical path. Full paired SHA256 values and exact source excerpts are also documented in [audio_release_compatibility.md](audio_release_compatibility.md).

| Compared source file | Comparison |
| --- | --- |
| `experiments/poc_audio/audio_flow.py` | Byte-identical |
| `experiments/poc_audio/audio_eval.py` | Byte-identical |
| `experiments/poc_audio/audio_classifier.py` | Byte-identical |
| `experiments/poc_audio/audio_data.py` | Byte-identical |
| `rafm/sources/radial_empirical.py` | Byte-identical |
| `rafm/utils/sphere.py` | Byte-identical |
| `rafm/flow_matching/sampler.py` | Byte-identical |
| `rafm/paths/spherical_geodesic.py` | Byte-identical |

The release manifest describes `d3006dc8ee61b2ab6e470115d3f21c778fefcf81` as the checkout holding the recovered artifacts and explicitly says that metadata did not record a training commit. It is **not a verified training commit**. The audit records `artifact_checkout.commit` separately, with `training_commit=null` and `training_commit_verified=false`. Local Git history adds the AudioMNIST scripts and reports together in commit `aa7a032`; it supplies no earlier evaluated-source snapshot proving the exact historical execution.

The inspected running evaluator has SHA256 `cc2ef04792bdce97157827847b84a77ffc82f5ac95cf484a40ea6213b5498301`, matching job 763355's recorded `evaluator_sha256`. The original `audio_eval.py` SHA256 is `ca4b1f0c788fcd3eef69b4970daf4a2cad540ee3fdcbe987e407f31a2f401004`. The classifier source SHA256 is `7af21994c8e7f428252ed566dbfae8274bf0b3368b96509dd76f7f65136f923a`. The source-audit JSON SHA256 is `bfe98eec3e7ccca6a336a62a93ca01c8c89850297b60b8396f7e994df5bdbe3f`.

The generation comparison found the following:

- Both evaluators call the same `build("fixed_spherical", ..., 0)`. Original R0 and the ECDF fitted to radius-normalized training tensors are preserved. Neither generation path draws initial radii from test data or replaces them with the post-hoc G.
- Both reset the CPU and evaluation-GPU RNG to seed 0 before drawing source radii and directions. Original `torch.manual_seed(0)` resets the visible devices; the new code explicitly resets the CPU and allocated GPU inside a restored RNG scope. The job requires exactly one visible GPU. Additional gain and source-replay draws have isolated, restored CPU RNG scopes and occur outside generation.
- Source sampling draws CPU radius uniforms before CPU sphere directions, then generation draws a separate normalized Gaussian direction tensor on the evaluation GPU. Both paths use `r*u0/u0.norm(...)` in the same order.
- Both generate 2,000 labels as 200 repetitions of each digit in order 0 through 9. Generation batch size is 128, including the same final partial batch.
- The RK4 inner velocity function and entire iteration body are AST-identical after renaming `nfe→RK4_STEPS`, `v→velocity`, and `y→labels`. This comparison preserves arithmetic association and multiplication/addition ordering. Both use 40 steps, four model calls per step, the same tangent projection, and no intermediate state renormalization.
- Both use the same UNet constructor and EMA state dictionary, evaluation mode, and Float32 evaluation path without autocast or compilation. The metadata depth value does not affect UNet construction: `make_model` forwards depth only to `VelNet`.

The classifier comparison found identical unit-norm STFT preprocessing, input shape `(2000,2,129,63)`, class labels, `Clf` architecture, evaluation mode, and direct full-batch inference. Original evaluation calls `Clf(Y)` twice, once for predictions and once for confidence; the new evaluator stores the first logits and reuses them for both summaries, then separately evaluates X. The classifier uses GroupNorm and per-sample normalization, with no dropout or running-statistics layers. This call-structure difference does not change the specified prediction calculation. Computing X first also leaves Y intact: `apply_gains` uses out-of-place normalization and multiplication. Both evaluators apply `argmax(1)` and the same legacy Float32 accuracy/rounding calculation.

Compute-node verification has already passed for all three EMA checkpoints before generation. The [attempt-2 compatibility report](../outputs_audio_gain/fixed_spherical_empirical_gain_v1_attempt2/compatibility.json), SHA256 `75c49ce2c0c46f0a41831c0cab3fe362de39078fa63f52cfb0343d1bf4f5ecc6`, records strict state-dictionary loads, step 24,000, 27,896,802 parameters, 10 classes, and angular mode false. Missing legacy metadata fields are resolved through the original evaluator's explicit defaults while the original metadata remains unchanged.

That report also records the verified local CPU split: `torch.Generator().manual_seed(0)` followed by `torch.randperm(12000)`, with the first 10,200 permuted indices used for generator training and the remaining 1,800 internally held out. The external test file contains 3,000 separate examples. The training-index SHA256 is `08a2f65d538cee711cc1dfaf7ec69ecc7ce95dbebe45702839654dbc107c92cd`; the internal held-out-index SHA256 is `79b1cb53ca272949bb52113f6ee99f4e2530f4014788d76ea2370a091233cb92`, both over contiguous little-endian int64 bytes. The empirical-gain ECDF fits raw norms at the generator-training indices only. Recomputed R0 passes comparison with the released `2.2613651752471924` value.

The supplied [RAFM_ICLR_2027.pdf](/mnt/vast01/users/fouad.oubari/msgm/rafm_review_inputs_20260909.qI0bZo/RAFM_ICLR_2027.pdf), printed page 28, Appendix C.2, reports Python 3.10.19, PyTorch 2.6.0+cu124, CUDA 12.4, and cuDNN 9.1.0. Printed page 29, Appendix C.3, reports Windows 11 and an NVIDIA RTX 2000 Ada Generation GPU with 16 GB memory. C.2 continues on page 29 with Float32 parameters and BF16 autocast during AudioMNIST training. The inspected original evaluation code does not enable autocast; training precision must not be conflated with evaluation precision. The completed execution used Python 3.10.12, PyTorch 2.7.1+rocm6.3, NumPy 2.2.6, and SciPy 1.15.3, as recorded in [environment_packages.json](../outputs_audio_gain/release_verification/environment_packages.json), on an AMD Instinct MI210. The aggregate records HIP version `6.3.42131-fa1d09cbd`. These platform and version differences are possible context for non-identical numerical outputs, **not a demonstrated cause** of the accuracy discrepancies. This audit has not compared identical historical inputs across those environments.

The precise historical artifacts needed to isolate the remaining discrepancy are:

1. The original per-seed `eval.json` files and exact evaluation commands, including checkpoint step, number of samples, generation batch size, solver steps, class count, classifier path, and seed. The local aggregate establishes the order 8925, 1234, 7 and rounded metrics, but the corresponding fixed-spherical per-seed evaluation files are absent locally.
2. Original full generated tensors Y with ordered labels, classifier logits and predictions, and exact correct counts. Classifying the same historical Y would separate classifier/output-processing differences from generation differences; comparing individual logits and margins would identify which samples differ.
3. Original initial tensors x0, or the original CPU radii, GPU direction draws, and RNG states immediately before sampling. These distinguish random-draw differences from integration differences even when both executions specify sample seed 0.
4. Hashes tying the historical evaluation to the exact EMA checkpoint, classifier checkpoint, nominal training/test tensors, and evaluated source snapshot. Authenticated release hashes establish the recovered files' identity, but do not independently prove which classifier file or source revision produced an earlier rounded report.
5. The original runtime/package manifest and execution settings, including device, driver, CUDA/cuDNN versions, default dtype, autocast/TF32/determinism settings, and any compilation or backend overrides. These would support a controlled comparison after the original artifacts are available.

The paired empirical-gain invariance result and the historical reproduction discrepancy are separate measured facts. Preserve both. The completed three-seed evaluation establishes unchanged predictions for all measured Y→X pairs and improved energy fit; it does not establish exact reproduction of historical directions, logits, or the published three-seed accuracy.
